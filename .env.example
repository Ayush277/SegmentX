# Copy to .env and fill values
PORT=3002
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=segmentx_db
# Optional: you can also set a DATABASE_URL/DB_URI like postgres://user:pass@host:5432/dbname
DB_URI=postgresql://postgres:@Aykmomis111000@db.udjebrelfemqtbrejtkh.supabase.co:5432/postgres
# Optional: Supabase client (auth/storage/realtime). Add these to your local `.env`.
# DO NOT commit real keys into git. Replace values with your project's credentials.
SUPABASE_URL=https://udjebrelfemqtbrejtkh.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_key_here # keep secret (server only)
# or SUPABASE_ANON_KEY=your_anon_key     # for client-limited usage

# SECURITY: Never commit real keys into git. If you shared keys publicly, rotate them in
# the Supabase dashboard immediately and store new keys in a local `.env` file.

# NOTE: This file must NOT contain real secrets. If you pasted real keys here,
# remove them and rotate the keys in the Supabase dashboard.
